Symlink the package to global node_modules to make `tools` runnable
`npm link`

Symlink the `zsh` folder to `oh-my-zsh`s custom plugin folder for cli auto-complete
Add `tools` to `plugins  = ()` in `.zshrc`
